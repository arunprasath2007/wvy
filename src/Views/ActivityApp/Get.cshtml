@model ActivityAppModel
@{
    Title = "Activity";
}

@section navbar_middle {
<div class="navbar-middle">
    <a href="@WeavyContext.Space.Url()" class="navbar-text">
        @WeavyContext.Space.Name
        @if (WeavyContext.Space.Teamname != null) {
            <small class="d-none d-sm-inline">@@@WeavyContext.Space.Teamname</small>
        }
    </a>
    @Html.StarToggle(WeavyContext.Space)
</div>
}

@section navbar_secondary {
    @{ Html.RenderPartial("_SpaceTabs"); }
}

<div class="container-fluid my-3">
    @if (Model.Activities.Any()) {
        <div class="list-group mb-4" id="infinite-scroll-target">
            @{ Html.RenderPartial("~/Views/Content/_ListItems.cshtml", Model.Activities); }
        </div>
        @Html.ScrollMore(Model.Activities)
    } else {
        <p class="empty text-center text-muted py-4">
            Nothing’s happened yet! But as soon as you get the ball rolling, everything will be summarized for you right here.
        </p>
    }

</div>

@section footer {
    @{
        Html.RenderPartial("_DrawerSpace");
        Html.RenderPartial("_MembersAddModal");
    }
}
