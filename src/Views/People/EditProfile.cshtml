@model User
@{
    Title = "People";
}


@section navbar_left {
    <div class="navbar-icons">
        <a class="btn btn-icon" href="@Model.Url()">@Html.SvgIcon("arrow-left")</a>
    </div>
}

@section navbar_secondary {
    @Html.Partial("_Navbar", Model)
}

<div class="container-fluid mt-3">
    @using (Html.BeginForm(nameof(PeopleController.UpdateProfile), typeof(PeopleController).ControllerName(), new { id = Model.Id }, FormMethod.Post, new { id = "update-profile-form", autocomplete = "off", data_turboform = "", novalidate = "" })) {
        @Html.HttpMethodOverride(HttpVerbs.Put)
        @Html.AntiForgeryToken()
        @Html.EditorsForModel(Model.Profile)
        <div class="form-buttons">
            <button type="submit" class="btn btn-primary btn-load waves-effect" title="Save">Save</button>
            <a href="@Model.Url()" class="btn btn-outline-secondary mr-auto waves-effect">Cancel</a>
        </div>
    }
</div>
