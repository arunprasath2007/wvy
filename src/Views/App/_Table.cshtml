@model SearchResult<IContent, ContentQuery>

@if (Model.Any()) {
    foreach (var content in Model) {
        Html.RenderPartialContent(ContentView.Table, content);
    }
    
    <tr class="scroll-next d-none" data-next="@Url.ScrollNext(Model)">
        <td colspan="6"></td>
    </tr>
    
}
