@model Space
@{
    Title = "Edit space";
    Back = Model.Url();
}

@section navbar_secondary {
    @Html.Partial("_Navbar", Model)
}

<div class="container my-3">
    @using (Html.BeginForm(nameof(SpaceController.Update), typeof(SpaceController).ControllerName(), new { id = Model.Id }, FormMethod.Post, new { autocomplete = "off", data_turboform = "", novalidate = "" })) {
        @Html.HttpMethodOverride(HttpVerbs.Put)

        <div class="text-center">
            @Html.EditorsForModel(include: new string[] { nameof(Model.Avatar) }, formLabels: false)
        </div>

        @Html.EditorsForModel(exclude: new string[] { nameof(Model.Avatar) })

        <div class="form-buttons">
            <button type="submit" name="button" value="@(nameof(SpaceController.Update))" class="btn btn-primary btn-load waves-effect" title="Save">Save</button>
            <a href="@Back" class="btn btn-outline-secondary mr-auto waves-effect">Cancel</a>
        </div>

    }
</div>
