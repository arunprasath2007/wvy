@model Space
@{
    Title = "Edit space";
    Back = Model.Url();
    List<AppSelectionModel> apps = (List<AppSelectionModel>)ViewBag.AppSelection;
}

@section navbar_secondary {
    @Html.Partial("_Navbar", Model)
}

<div class="container my-3">
    @using (Html.BeginForm(nameof(SpaceController.Update), typeof(SpaceController).ControllerName(), new { id = Model.Id }, FormMethod.Post, new { autocomplete = "off", data_turboform = "", novalidate = "" })) {
        @Html.HttpMethodOverride(HttpVerbs.Put)

        <div class="text-center">
            @Html.EditorFor(x => Model.Avatar, additionalViewData: new { AvatarFor = Model })
        </div>

        @Html.EditorsForModel()

        <h3>Apps</h3>
        <table class="table">
            @Html.EditorFor(model => apps, "AppSelection")
        </table>

        <div class="form-buttons">
            <button type="submit" name="button" value="@(nameof(SpaceController.Update))" class="btn btn-primary btn-load" title="Save">Save</button>
            <a href="@Back" class="btn btn-outline-secondary mr-auto">Cancel</a>
        </div>

    }
</div>
